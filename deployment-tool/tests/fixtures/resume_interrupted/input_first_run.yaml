# First run - partial completion (interrupted after email step)
responses:
  - "https://avatar.example.com"   # PUBLIC_URL
  - "production"                   # ENV_NAME
  - "MyCompany"                    # ORGANIZATION_NAME
  - "smtp"                         # MAIL_PROVIDER
  - "smtp.example.com"            # SMTP_HOST
  - "587"                          # SMTP_PORT
  - "noreply@example.com"         # SMTP_SENDER_EMAIL
  # Interrupted here - remaining prompts not answered
